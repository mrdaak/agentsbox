#!/usr/bin/env bash
# codex-container — disable nested Codex sandboxing for `codex exec` only
set -euo pipefail

REAL_CODEX="/root/.nix-profile/bin/codex"

if [[ "${1:-}" == "exec" ]]; then
    exec "$REAL_CODEX" --dangerously-bypass-approvals-and-sandbox "$@"
fi

exec "$REAL_CODEX" "$@"
