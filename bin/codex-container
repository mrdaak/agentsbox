#!/usr/bin/env bash
# codex-container — run Codex unsandboxed; the Podman container is the boundary
set -euo pipefail

REAL_CODEX="/root/.nix-profile/bin/codex"

exec "$REAL_CODEX" --dangerously-bypass-approvals-and-sandbox "$@"
